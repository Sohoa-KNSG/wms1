{
  "name": "wms-edge-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "UC05 hardware bridge for Raspberry Pi 4 scale and TSPL printer integration",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/services/scale-service.js && node --check src/services/printer-service.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@serialport/parser-readline": "^13.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.0",
    "express": "^5.0.0",
    "serialport": "^13.0.0"
  }
}
